# Agentspace CLI Manifest Projection Daily Rollup

Date: 2026-03-08

- [x] `agentspace-tooling` owner surface'i `tools.ts` + `cli-projection.ts` modeline cekildi.
- [x] `agentspace-cli` projection-driven `--help`, `manifest cli|get|show`, `--version` ve generated sugar browse yuzeyine kavustu.
- [x] `agentspace-tests` icine CLI browse/help regression suite eklendi.
